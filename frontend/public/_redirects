# Prerendered routes (/, /all, /about, /peak/<slug>, ...) are emitted as static
# files; Pages serves a matching static asset before consulting this splat, so
# prerendered pages are NOT shadowed. The splat only fires for paths with no
# prerendered file — it serves the prerendered home page, which then client-routes
# (unknown URLs fall through Angular's ** route to home).
# Verify on the deploy preview: view-source of a peak URL shows its baked <title>/<h1>.
/*  /index.html  200
